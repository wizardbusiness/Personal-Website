<main
  class="flex h-full w-full flex-col items-center overflow-scroll p-8 pb-0 pt-4"
>
  <header class="flex w-full">
    <slot name="nav" />
  </header>

  <slot name="subheader" />
  <div
    data-info-container
    class="animate-impact flex h-full flex-col gap-8 bg-white p-8 text-gray-600 shadow-xl"
  >
    <slot name="about" />
    <slot name="portfolio" />
    <slot name="tech" />
  </div>
</main>
