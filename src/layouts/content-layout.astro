<main class="flex h-full w-full flex-col items-center p-8 pb-0 pt-4">
  <header class="flex w-full">
    <slot name="nav" />
  </header>

  <slot name="subheader" />
  <div
    data-info-container
    class="bg-blue-smoke flex h-full w-[70vw] animate-impact flex-col gap-8 overflow-scroll p-8 text-gray-200 shadow-xl"
  >
    <slot name="about" />
    <slot name="portfolio" />
    <slot name="tech" />
  </div>
</main>
