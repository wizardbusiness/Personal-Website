---
import MainLayout from "../layouts/main-layout.astro";
import Nav from "../components/Nav.astro";
---

<MainLayout>
  <div slot="page" class="flex h-screen items-center justify-center">
    <iframe
      allowtransparency="true"
      style="opacity: 0"
      src="https://drive.google.com/file/d/1243MRw12jWwavSvlrgV2EI2oTzAuGZDE/preview?#view=fitH"
      width="100%"
      height="100%"></iframe>
  </div>
</MainLayout>

<script>
  const iframe = document.querySelector("iframe");
  iframe.onload = () => {
    iframe.classList.add("animate-fade-in-resume");
  };

  // listen for mouse activity
  window.addEventListener("mousemove", () => {
    showNav();
  });
  // get nav current position
  // slide nav down to max from current position
  function showNav() {
    const nav = document.querySelector(".nav-container");
    nav.classList.add("animate-slide-down");
    // nav.getBoundingClientRect();
  }
  // set timeout from last activity
  // when timeout expires slide nav up to hidden position
</script>
